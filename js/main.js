document.addEventListener('DOMContentLoaded', function () {
  particleground(document.getElementById('particles'), {
    dotColor: '#000',
    lineColor: '#000'
  });
  var particles = document.getElementById('particles');
  head.style.marginTop = - intro.offsetHeight / 2 + 'px';
}, false);